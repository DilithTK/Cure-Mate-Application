name: curemate_app
description: >
  A Flutter healthcare app for prescription uploads, pharmacy responses,
  reminders, maps, and AI medicine support.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Security / utils
  http: ^1.2.0
  flutter_dotenv: ^5.1.0
  path_provider: ^2.1.2

  # Features
  image_picker: ^1.0.0
  geolocator: ^10.1.0
  google_maps_flutter: ^2.3.0
  photo_view: ^0.15.0
  google_mlkit_text_recognition: ^0.13.0

  timezone: ^0.9.2

  intl: ^0.19.0

  # Firebase (STABLE MATCH - IMPORTANT)
  firebase_core: ^3.6.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  firebase_app_check: ^0.3.2+10
  firebase_messaging: ^15.0.0
  flutter_local_notifications: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"
  adaptive_icon_background: "#588486"
  adaptive_icon_foreground: "assets/images/app_icon_foreground.png"

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/app_icon.png
    - assets/images/app_icon_foreground.png
    - assets/images/BackgroundImage.png
    - .env
